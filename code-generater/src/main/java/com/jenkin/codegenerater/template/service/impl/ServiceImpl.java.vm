package com.jenkin.men ${classNameLower}vice.template.service.impl;

import com.baomidou.mybatisplus.extension.plugins.pagination.Page;
import com.baomidou.mybatisplus.extension.service.impl.ServiceImpl;
import com.jenkin.common.config.MyQueryWrapper;
import ${entityPackageName}.dtos.${moduleName}.${className}Dto;
import ${entityPackageName}.pos.${moduleName}.MenuPo;
import ${entityPackageName}.pos.${moduleName}.${className}Po;
import ${entityPackageName}.qos.BaseQo;
import ${entityPackageName}.qos.${moduleName}.${className}Qo;
import com.jenkin.common.utils.BeanUtils;
import com.jenkin.common.utils.SimpleQuery;

import org.springframework.stereotype.Service;


/**
 * @author jenkin
 * @className MenuServiceImpl
 * @description TODO
 * @date 2020/12/9 15:58
 */
@Service
public class ${className}ServiceImpl extends ServiceImpl<${className}Mapper, MenuPo> implements ${className}Service {
    @Override
    public ${className}Dto getById(Integer id) {
        return BeanUtils.map(getById(id),${className}Dto.class);
    }

    /**
     * 分页获取用户
     *
     * @param  ${classNameLower}
     */
    @Override
    public Page<${className}Dto> listByPage(BaseQo<${className}Qo>  ${classNameLower}) {
        SimpleQuery<${className}Po> simpleQuery = SimpleQuery.builder( ${classNameLower},this).sort();
        MyQueryWrapper<${className}Po> query = simpleQuery.getQuery();
        return simpleQuery.page(${className}Dto.class);
    }

    /**
     * 保存用户信息
     *
     * @param  ${classNameLower}
     * @return
     */
    @Override
    public ${className}Dto save${className}Info(${className}Dto  ${classNameLower}) {
        saveOrUpdate( ${classNameLower});
        return  ${classNameLower};
    }

}
